# Normalize line endings: LF everywhere (matches ruff line-ending = "lf")
* text=auto eol=lf

# Windows scripts keep CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binaries
*.png binary
*.ico binary
